services:
  finsy:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FINSY_DB=/app/app/db/finsy.db
      - RISK_MODEL=/app/app/models/risk_model.pkl
    volumes:
      - ./app:/app/app
